<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView android:text="Выберите категорию" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onBtnExpensesClick"
        android:text="Траты"
        android:id="@+id/btnExpenses"
        android:layout_marginTop="63dp"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onBtnIncomingClick"
        android:text="Доходы"
        android:id="@+id/btnIncoming"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/btnOther"
        android:layout_alignStart="@+id/btnOther" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onBtnOtherClick"
        android:text="Другое"
        android:id="@+id/btnOther"
        android:layout_below="@+id/btnIncoming"
        android:layout_alignLeft="@+id/btnExpenses"
        android:layout_alignStart="@+id/btnExpenses"
        android:layout_marginTop="85dp" />

</RelativeLayout>
